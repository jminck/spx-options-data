output {
    elasticsearch {
	hosts => [ "${LL_ES_HOST:127.0.0.1}" ]
	index => "%{type}-%{+YYYY.MM.dd}"
  user => "${ES_USER:es_user}"
  password => "${ES_PASSWORD:es_user_password}"
    }
}

output {
  stdout {
    codec => rubydebug {
    }
  }
}
